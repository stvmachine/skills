---
phase: 01-rename-and-publish
plan_id: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - cmd/stevmachine-skills/main.go
  - cmd/stevmachine-skills/cmd_install.go
  - cmd/stevmachine-skills/cmd_env.go
  - cmd/stevmachine-skills/cmd_doctor.go
  - cmd/stevmachine-skills/cmd_list.go
  - internal/vault/vault.go
  - internal/vault/vault_test.go
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02
  - RENAME-03
must_haves:
  truths:
    - go.mod declares `module github.com/stevmachine/skills`
    - All internal Go imports use `github.com/stevmachine/skills/...`
    - The binary directory is `cmd/stevmachine-skills`
    - The CLI usage and TUI titles say `stevmachine-skills` / `Stevmachine Skills`
    - `defaultSkills()` returns `stevmachine-*` names
    - `vault.New()` sets `EnvDir` to `~/.stevmachine-skills`
    - `cmd_doctor.go` reads `STEVMACHINE_TICKET_DIR`
  artifacts:
    - go.mod with module `github.com/stevmachine/skills`
    - cmd/stevmachine-skills/main.go, cmd_install.go, cmd_env.go, cmd_doctor.go, cmd_list.go
    - internal/vault/vault.go and vault_test.go with `~/.stevmachine-skills`
  key_links:
    - "`defaultSkills()` values must match the embedded skill directory names that will be renamed in Plan 02"
    - "`cmd_install.go` embed path `claude-plugin/skills/<name>` must match the renamed directories in Plan 02"
---

# Plan 01: String Sweep and Code Rename

## Objective
Rename the Go module, CLI directory, imports, vault path, default skill names, and user-facing CLI strings so that no `medtasker`, `Medtasker`, or `nimblic` references remain in the Go source. This is the foundation for all downstream rename work.

## Execution Context
@/Users/estvmachine/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/estvmachine/.config/opencode/gsd-core/templates/summary.md

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@go.mod
@cmd/medtasker-skills/main.go
@cmd/medtasker-skills/cmd_install.go
@cmd/medtasker-skills/cmd_env.go
@cmd/medtasker-skills/cmd_doctor.go
@cmd/medtasker-skills/cmd_list.go
@internal/vault/vault.go
@internal/vault/vault_test.go

## Tasks

<task type="auto">
  <name>Task 1: Rename Go module path and package imports</name>
  <files>go.mod, cmd/stevmachine-skills/main.go, cmd/stevmachine-skills/cmd_install.go, cmd/stevmachine-skills/cmd_env.go, cmd/stevmachine-skills/cmd_doctor.go, cmd/stevmachine-skills/cmd_list.go</files>
  <read_first>
    - go.mod
    - cmd/medtasker-skills/cmd_install.go
    - cmd/medtasker-skills/cmd_env.go
    - cmd/medtasker-skills/cmd_doctor.go
    - cmd/medtasker-skills/cmd_list.go
  </read_first>
  <action>
    Change the module path in go.mod from `github.com/nimblic/medtasker-skills` to `github.com/stevmachine/skills`. Replace every `github.com/nimblic/medtasker-skills/internal/mcp` and `github.com/nimblic/medtasker-skills/packages` import in the Go source files with `github.com/stevmachine/skills/internal/mcp` and `github.com/stevmachine/skills/packages` respectively. Do not alter any third-party import paths.
  </action>
  <acceptance_criteria>
    - go.mod contains exactly `module github.com/stevmachine/skills`
    - No .go file contains `github.com/nimblic/medtasker-skills`
    - No .go file contains `nimblic`
  </acceptance_criteria>
  <verify>
    <automated>grep -R 'github.com/nimblic/medtasker-skills' --include='*.go' --include='go.mod' . ; test $? -ne 0</automated>
  </verify>
  <done>Go module path and all internal package imports are renamed to `github.com/stevmachine/skills`.</done>
</task>

<task type="auto">
  <name>Task 2: Rename CLI directory and CLI-facing strings</name>
  <files>cmd/stevmachine-skills/main.go, cmd/stevmachine-skills/cmd_install.go, cmd/stevmachine-skills/cmd_env.go, cmd/stevmachine-skills/cmd_doctor.go</files>
  <read_first>
    - cmd/medtasker-skills/main.go
    - cmd/medtasker-skills/cmd_install.go
    - cmd/medtasker-skills/cmd_env.go
    - cmd/medtasker-skills/cmd_doctor.go
  </read_first>
  <action>
    Rename the directory `cmd/medtasker-skills` to `cmd/stevmachine-skills`. In `main.go`, update the usage header from `Usage: medtasker-skills <command>` to `Usage: stevmachine-skills <command>`. In `cmd_env.go`, update the usage error string to `Usage: stevmachine-skills env set [-e env] KEY VALUE` and update the wizard title from `Medtasker Skills Setup` to `Stevmachine Skills Setup`. In `cmd_install.go`, update the TUI title and fallback print from `Medtasker Skills` to `Stevmachine Skills`, and update `defaultSkills()` to return `stevmachine-jira`, `stevmachine-jira-markup`, `stevmachine-jira-ticket-transition`, and `commit`. In `cmd_doctor.go`, rename the `MEDTASKER_TICKET_DIR` environment variable to `STEVMACHINE_TICKET_DIR` and update the default label display.
  </action>
  <acceptance_criteria>
    - Directory `cmd/stevmachine-skills` exists and `cmd/medtasker-skills` does not
    - `main.go` usage text says `Usage: stevmachine-skills <command>`
    - `cmd_install.go` contains `defaultSkills()` returning `stevmachine-jira`, `stevmachine-jira-markup`, `stevmachine-jira-ticket-transition`, `commit`
    - `cmd_doctor.go` contains `STEVMACHINE_TICKET_DIR`
    - No `medtasker-skills` or `Medtasker Skills` remains in the renamed `cmd/stevmachine-skills` files
  </acceptance_criteria>
  <verify>
    <automated>test -d cmd/stevmachine-skills && test ! -d cmd/medtasker-skills && ! grep -R 'medtasker-skills\|Medtasker Skills' --include='*.go' cmd/stevmachine-skills</automated>
  </verify>
  <done>CLI directory, usage strings, wizard title, and default skill names are renamed.</done>
</task>

<task type="auto">
  <name>Task 3: Rename dotenvx vault directory</name>
  <files>internal/vault/vault.go, internal/vault/vault_test.go</files>
  <read_first>
    - internal/vault/vault.go
    - internal/vault/vault_test.go
  </read_first>
  <action>
    In `internal/vault/vault.go`, change the `EnvDir` default from `filepath.Join(home, ".medtasker-skills")` to `filepath.Join(home, ".stevmachine-skills")`. In `internal/vault/vault_test.go`, update the `TestManagerPaths` assertion so the expected suffix is `.stevmachine-skills` instead of `.medtasker-skills`.
  </action>
  <acceptance_criteria>
    - `vault.go` contains `.stevmachine-skills` as the default vault directory
    - `vault_test.go` asserts the suffix `.stevmachine-skills`
    - Neither file contains `.medtasker-skills` or `medtasker`
  </acceptance_criteria>
  <verify>
    <automated>grep -R '\.medtasker-skills\|medtasker' --include='*.go' internal/vault ; test $? -ne 0</automated>
  </verify>
  <done>Vault default directory is renamed to `~/.stevmachine-skills`.</done>
</task>

## must_haves

truths:
  - go.mod declares `module github.com/stevmachine/skills`
  - All internal Go imports use `github.com/stevmachine/skills/...`
  - The binary directory is `cmd/stevmachine-skills`
  - The CLI usage and TUI titles say `stevmachine-skills` / `Stevmachine Skills`
  - `defaultSkills()` returns `stevmachine-*` names
  - `vault.New()` sets `EnvDir` to `~/.stevmachine-skills`
  - `cmd_doctor.go` reads `STEVMACHINE_TICKET_DIR`

artifacts:
  - go.mod with module `github.com/stevmachine/skills`
  - cmd/stevmachine-skills/main.go, cmd_install.go, cmd_env.go, cmd_doctor.go, cmd_list.go
  - internal/vault/vault.go and vault_test.go with `~/.stevmachine-skills`

key_links:
  - `defaultSkills()` values must match the embedded skill directory names that will be renamed in Plan 02
  - `cmd_install.go` embed path `claude-plugin/skills/<name>` must match the renamed directories in Plan 02

## Artifacts this phase produces

- `module github.com/stevmachine/skills` in `go.mod`
- `cmd/stevmachine-skills` binary entry directory
- `defaultSkills()` returning `["stevmachine-jira", "stevmachine-jira-markup", "stevmachine-jira-ticket-transition", "commit"]`
- `vault.Manager` with `EnvDir` defaulting to `~/.stevmachine-skills`
- `STEVMACHINE_TICKET_DIR` env var read in `cmd_doctor.go`

## Verification

- `grep -R 'github.com/nimblic/medtasker-skills' --include='*.go' --include='go.mod' .` returns no matches
- `test -d cmd/stevmachine-skills && test ! -d cmd/medtasker-skills`
- `go build ./cmd/stevmachine-skills` succeeds after Plan 02 directory renames (deferred to Plan 03)

## Output

Create `.planning/phases/01-rename-and-publish/01-01-SUMMARY.md` when done.
