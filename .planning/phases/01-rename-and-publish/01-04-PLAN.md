---
phase: 01-rename-and-publish
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
  - DESIGN.md
  - docs/INSTALL.md
  - docs/TROUBLESHOOTING.md
  - docs/ADR-001-dotenvx.md
  - docs/ADR-002-mcp-servers.md
  - docs/ADR-003-rtk.md
  - docs/ADR-004-beads.md
  - tapes/setup-flow.sh
  - tapes/demo.tape
  - tapes/generate.sh
  - docs/assets/demo.gif
  - docs/assets/setup-flow.png
autonomous: true
requirements:
  - RENAME-02
must_haves:
  truths:
    - README.md contains no employer branding
    - DESIGN.md and all ADRs contain no employer branding
    - `docs/INSTALL.md` and `docs/TROUBLESHOOTING.md` contain no employer branding
    - All clone URLs point to `git@github.com:stevmachine/skills.git`
    - All vault paths and env vars reference `stevmachine-skills` / `STEVMACHINE_*`
    - Tape scripts and generation scripts reference the renamed CLI and paths
  artifacts:
    - README.md
    - DESIGN.md
    - docs/INSTALL.md
    - docs/TROUBLESHOOTING.md
    - docs/ADR-001-dotenvx.md
    - docs/ADR-002-mcp-servers.md
    - docs/ADR-003-rtk.md
    - docs/ADR-004-beads.md
    - tapes/setup-flow.sh
    - tapes/demo.tape
    - tapes/generate.sh
  key_links:
    - README clone URL → GitHub remote created in Plan 05
    - Docs env vars → Go vault path from Plan 01
---

# Plan 04: README and Documentation Rewrite

## Objective
Rewrite all user-facing documentation and demo assets to remove `medtasker`, `Medtasker`, and `nimblic` references and align the clone URLs, skill names, and env vars with the new `stevmachine-skills` identity.

## Execution Context
@/Users/estvmachine/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/estvmachine/.config/opencode/gsd-core/templates/summary.md

## Context
@.planning/phases/01-rename-and-publish/01-01-SUMMARY.md
@README.md
@DESIGN.md
@docs/INSTALL.md
@docs/TROUBLESHOOTING.md
@docs/ADR-001-dotenvx.md
@docs/ADR-002-mcp-servers.md
@docs/ADR-003-rtk.md
@docs/ADR-004-beads.md
@tapes/setup-flow.sh
@tapes/demo.tape
@tapes/generate.sh

## Tasks

<task type="auto">
  <name>Task 1: Rewrite README.md and remove or replace branding assets</name>
  <files>README.md, docs/assets/demo.gif, docs/assets/setup-flow.png</files>
  <read_first>
    - README.md
  </read_first>
  <action>
    Replace the README title `# Medtasker Skills` with `# stevmachine-skills`. Replace every `medtasker-skills` with `stevmachine-skills`, `~/.medtasker-skills` with `~/.stevmachine-skills`, and `MEDTASKER_VAULT` with `STEVMACHINE_VAULT`. Replace the clone URL with `git@github.com:stevmachine/skills.git` and update the SSH/private-repo note to a public-repo note. Update the available skills table to list `stevmachine-jira`, `stevmachine-jira-markup`, `stevmachine-jira-ticket-transition`, `commit`, and `run-stevmachine-skills`. Remove the `![demo](docs/assets/demo.gif)` and `![setup flow](docs/assets/setup-flow.png)` image references if the assets cannot be regenerated in this phase; otherwise delete the old GIF/PNG and regenerate them with `tapes/generate.sh` after the tape scripts are updated. If the assets are removed, add a short placeholder note explaining that new assets will be generated in a later phase.
  </action>
  <acceptance_criteria>
    - README.md contains no `medtasker`, `Medtasker`, or `nimblic`
    - README.md contains `stevmachine-skills install`, `~/.stevmachine-skills`, `git@github.com:stevmachine/skills.git`, and `STEVMACHINE_VAULT`
    - The available skills table lists the new `stevmachine-*` names
    - `docs/assets/` contains no old-branding image references or the assets are removed
  </acceptance_criteria>
  <verify>
    <automated>grep -ci 'medtasker\|nimblic' README.md ; test $? -ne 0</automated>
  </verify>
  <done>README.md is fully rewritten for the new project name and assets are handled.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DESIGN.md and ADRs</name>
  <files>DESIGN.md, docs/ADR-001-dotenvx.md, docs/ADR-002-mcp-servers.md, docs/ADR-003-rtk.md, docs/ADR-004-beads.md</files>
  <read_first>
    - DESIGN.md
    - docs/ADR-001-dotenvx.md
    - docs/ADR-002-mcp-servers.md
    - docs/ADR-003-rtk.md
    - docs/ADR-004-beads.md
  </read_first>
  <action>
    Replace all occurrences of `Medtasker Skills`, `medtasker-skills`, `medtasker-jira`, `medtasker-jira-markup`, `medtasker-jira-ticket-transition`, `medtasker-frontends`, and `nimblic` with the new names (`Stevmachine Skills`, `stevmachine-skills`, `stevmachine-jira`, etc.) or with generic placeholders. Replace `MEDTASKER_SKILLS_DEBUG` with `STEVMACHINE_SKILLS_DEBUG`. Replace `~/.medtasker-skills` with `~/.stevmachine-skills`. Replace the clone URL in DESIGN.md with `git@github.com:stevmachine/skills.git`. Remove or replace the `Authors: Medtasker Engineering Team` line. Ensure the ADRs no longer claim the project is for an employer-specific distribution system; instead describe it as a personal skill distribution system owned by Steve.
  </action>
  <acceptance_criteria>
    - None of DESIGN.md or the ADRs contain `medtasker`, `Medtasker`, or `nimblic`
    - Env vars and skill names in examples are updated to `STEVMACHINE_*` and `stevmachine-*`
    - The clone URL in DESIGN.md points to `git@github.com:stevmachine/skills.git`
  </acceptance_criteria>
  <verify>
    <automated>grep -ri 'medtasker\|nimblic' DESIGN.md docs/ADR-001-dotenvx.md docs/ADR-002-mcp-servers.md docs/ADR-003-rtk.md docs/ADR-004-beads.md ; test $? -ne 0</automated>
  </verify>
  <done>Architecture and ADR documents reflect the new project identity and ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Update installation docs and tape scripts</name>
  <files>docs/INSTALL.md, docs/TROUBLESHOOTING.md, tapes/setup-flow.sh, tapes/demo.tape, tapes/generate.sh</files>
  <read_first>
    - docs/INSTALL.md
    - docs/TROUBLESHOOTING.md
    - tapes/setup-flow.sh
    - tapes/demo.tape
    - tapes/generate.sh
  </read_first>
  <action>
    In `docs/INSTALL.md`, replace `medtasker-skills` with `stevmachine-skills`, `~/.medtasker-skills` with `~/.stevmachine-skills`, the clone URL with `git@github.com:stevmachine/skills.git`, and remove the private-repo SSH note. In `docs/TROUBLESHOOTING.md`, apply the same replacements. In `tapes/setup-flow.sh`, replace the clone URL and all `medtasker-skills` / `~/.medtasker-skills` references. In `tapes/demo.tape`, replace all `medtasker-skills` commands with `stevmachine-skills` and `~/.medtasker-skills` with `~/.stevmachine-skills`. In `tapes/generate.sh`, replace the binary output name `medtasker-skills` with `stevmachine-skills` and the source path `cmd/medtasker-skills` with `cmd/stevmachine-skills`.
  </action>
  <acceptance_criteria>
    - `docs/INSTALL.md` and `docs/TROUBLESHOOTING.md` contain no `medtasker`, `Medtasker`, or `nimblic`
    - `tapes/setup-flow.sh`, `tapes/demo.tape`, and `tapes/generate.sh` contain no `medtasker` or `nimblic`
    - All clone URLs point to `git@github.com:stevmachine/skills.git`
    - All vault paths point to `~/.stevmachine-skills`
  </acceptance_criteria>
  <verify>
    <automated>grep -ci 'medtasker\|nimblic' docs/INSTALL.md docs/TROUBLESHOOTING.md tapes/setup-flow.sh tapes/demo.tape tapes/generate.sh ; test $? -ne 0</automated>
  </verify>
  <done>Installation docs and demo tape scripts are renamed and aligned with the new repo.</done>
</task>

## must_haves

truths:
  - README.md contains no employer branding
  - DESIGN.md and all ADRs contain no employer branding
  - `docs/INSTALL.md` and `docs/TROUBLESHOOTING.md` contain no employer branding
  - All clone URLs point to `git@github.com:stevmachine/skills.git`
  - All vault paths and env vars reference `stevmachine-skills` / `STEVMACHINE_*`
  - Tape scripts and generation scripts reference the renamed CLI and paths

artifacts:
  - README.md
  - DESIGN.md
  - docs/INSTALL.md
  - docs/TROUBLESHOOTING.md
  - docs/ADR-001-dotenvx.md
  - docs/ADR-002-mcp-servers.md
  - docs/ADR-003-rtk.md
  - docs/ADR-004-beads.md
  - tapes/setup-flow.sh
  - tapes/demo.tape
  - tapes/generate.sh

key_links:
  - README clone URL → GitHub remote created in Plan 05
  - Docs env vars → Go vault path from Plan 01

## Artifacts this phase produces

- Rewritten `README.md` as `stevmachine-skills`
- Rewritten `DESIGN.md` and ADRs with `stevmachine-skills` / `stevmachine-*` names
- Updated `docs/INSTALL.md` and `docs/TROUBLESHOOTING.md`
- Updated `tapes/setup-flow.sh`, `tapes/demo.tape`, and `tapes/generate.sh`
- Removed or regenerated branding assets in `docs/assets/`

## Verification

- `grep -ri 'medtasker\|nimblic' README.md DESIGN.md docs/ tapes/` returns no matches
- All clone URLs in README.md and docs/INSTALL.md point to `git@github.com:stevmachine/skills.git`
- All vault paths in README.md and docs point to `~/.stevmachine-skills`

## Output

Create `.planning/phases/01-rename-and-publish/01-04-SUMMARY.md` when done.
