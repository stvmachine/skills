---
phase: 01-rename-and-publish
plan_id: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - go.sum
  - .gitignore
  - stevmachine-skills
  - scripts/install.sh
  - scripts/verify-install.sh
autonomous: true
requirements:
  - RENAME-04
  - RENAME-05
must_haves:
  truths:
    - `go build -o stevmachine-skills ./cmd/stevmachine-skills` succeeds
    - `go test ./internal/mcp ./cmd/...` passes
    - The embedded smoke test reports `0 failed`
    - `scripts/verify-install.sh` exits 0
    - The generated `.mcp.json` still contains literal `${JIRA_API_TOKEN}` placeholders (no resolved secrets)
    - Standalone `scripts/install.sh` and `scripts/verify-install.sh` reference only the renamed project
  artifacts:
    - `stevmachine-skills` binary in the repo root
    - Updated `.gitignore` with the binary name
    - Updated `go.sum`
    - Updated `scripts/install.sh`
    - Updated `scripts/verify-install.sh`
  key_links:
    - Build output → smoke test → installed `stevmachine-jira` skill
    - Smoke test → `.mcp.json` placeholder invariant
    - `scripts/install.sh` → clone URL → GitHub repo created in Plan 05
---

# Plan 03: Build and Smoke Test

## Objective
Build the renamed CLI, run the unit and smoke tests, update the standalone install scripts, and fix any regressions so the end-to-end Claude Code installation flow still works.

## Execution Context
@/Users/estvmachine/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/estvmachine/.config/opencode/gsd-core/templates/summary.md

## Context
@.planning/phases/01-rename-and-publish/01-01-SUMMARY.md
@.planning/phases/01-rename-and-publish/01-02-SUMMARY.md
@go.mod
@cmd/stevmachine-skills/main.go
@packages/claude-plugin/skills/run-stevmachine-skills/smoke.sh
@scripts/install.sh
@scripts/verify-install.sh

## Tasks

<task type="auto">
  <name>Task 1: Tidy dependencies and build the renamed binary</name>
  <files>go.sum, .gitignore, stevmachine-skills</files>
  <read_first>
    - go.mod
    - cmd/stevmachine-skills/main.go
    - .gitignore
  </read_first>
  <action>
    Run `go mod tidy` to update `go.sum` for the new module path. Build the CLI with `go build -o stevmachine-skills ./cmd/stevmachine-skills`. Run `go test ./internal/mcp ./cmd/...` to verify the renamed code still passes unit tests. If `DOTENV_PRIVATE_KEY` is set in the environment, also run `go test ./internal/vault`; otherwise skip the vault package with a logged note. Ensure `.gitignore` contains an entry for the built binary `stevmachine-skills`; append it if absent so the binary is not committed.
  </action>
  <acceptance_criteria>
    - `go mod tidy` exits 0
    - `go build -o stevmachine-skills ./cmd/stevmachine-skills` exits 0 and produces an executable `stevmachine-skills` in the repo root
    - `go test ./internal/mcp ./cmd/...` passes
    - `.gitignore` contains `stevmachine-skills`
  </acceptance_criteria>
  <verify>
    <automated>go mod tidy && go build -o stevmachine-skills ./cmd/stevmachine-skills && test -x stevmachine-skills && grep -Fxq 'stevmachine-skills' .gitignore && go test ./internal/mcp ./cmd/...</automated>
  </verify>
  <done>The renamed binary builds and the core unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update standalone install and verify scripts</name>
  <files>scripts/install.sh, scripts/verify-install.sh</files>
  <read_first>
    - scripts/install.sh
    - scripts/verify-install.sh
  </read_first>
  <action>
    In `scripts/install.sh`, replace every `medtasker-skills` with `stevmachine-skills`, `~/.medtasker-skills` with `~/.stevmachine-skills`, and the clone URL with `git@github.com:stevmachine/skills.git`. Update the `go.mod` validation regex to check for `^module github.com/stevmachine/skills`. Update the post-install banner and usage text to `Stevmachine Skills`. In `scripts/verify-install.sh`, replace the binary name, vault directory, and all `medtasker` / `Medtasker` strings with `stevmachine` / `Stevmachine`.
  </action>
  <acceptance_criteria>
    - Neither `scripts/install.sh` nor `scripts/verify-install.sh` contains `medtasker`, `Medtasker`, or `nimblic`
    - `install.sh` validates `^module github.com/stevmachine/skills` and clones `git@github.com:stevmachine/skills.git`
    - `verify-install.sh` checks `stevmachine-skills` on PATH and `~/.stevmachine-skills` vault directory
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/install.sh && test -f scripts/verify-install.sh && ! grep -q 'medtasker\|Medtasker\|nimblic' scripts/install.sh scripts/verify-install.sh</automated>
  </verify>
  <done>Standalone install and verify scripts are fully renamed.</done>
</task>

<task type="auto">
  <name>Task 3: Run smoke and install verification</name>
  <files>stevmachine-skills, go.sum, packages/claude-plugin/skills/run-stevmachine-skills/smoke.sh, scripts/verify-install.sh</files>
  <read_first>
    - packages/claude-plugin/skills/run-stevmachine-skills/smoke.sh
    - scripts/verify-install.sh
    - cmd/stevmachine-skills/cmd_install.go
  </read_first>
  <action>
    Run the embedded smoke test from the repo root: `packages/claude-plugin/skills/run-stevmachine-skills/smoke.sh`. Then run `scripts/verify-install.sh`. If either script fails, inspect the output and fix the underlying source (for example, embed path mismatch, skill name mismatch, or MCP config placeholder issue). Re-run until both scripts exit 0. Confirm that the generated `.mcp.json` in the isolated test home contains the literal placeholder `${JIRA_API_TOKEN}` and not a resolved secret.
  </action>
  <acceptance_criteria>
    - `smoke.sh` reports `0 failed`
    - `scripts/verify-install.sh` exits 0 (warnings allowed, no failures)
    - The smoke test's isolated `~/.claude/.mcp.json` contains `"JIRA_API_TOKEN": "${JIRA_API_TOKEN}"`
  </acceptance_criteria>
  <verify>
    <automated>packages/claude-plugin/skills/run-stevmachine-skills/smoke.sh && ./scripts/verify-install.sh</automated>
  </verify>
  <done>The renamed CLI installs skills, writes the MCP config, and passes end-to-end verification.</done>
</task>

## must_haves

truths:
  - `go build -o stevmachine-skills ./cmd/stevmachine-skills` succeeds
  - `go test ./internal/mcp ./cmd/...` passes
  - The embedded smoke test reports `0 failed`
  - `scripts/verify-install.sh` exits 0
  - The generated `.mcp.json` still contains literal `${JIRA_API_TOKEN}` placeholders (no resolved secrets)
  - Standalone `scripts/install.sh` and `scripts/verify-install.sh` reference only the renamed project

artifacts:
  - `stevmachine-skills` binary in the repo root
  - Updated `.gitignore` with the binary name
  - Updated `go.sum`
  - Updated `scripts/install.sh`
  - Updated `scripts/verify-install.sh`

key_links:
  - Build output → smoke test → installed `stevmachine-jira` skill
  - Smoke test → `.mcp.json` placeholder invariant
  - `scripts/install.sh` → clone URL → GitHub repo created in Plan 05

## Artifacts this phase produces

- Built `stevmachine-skills` binary in the repo root
- `.gitignore` updated to ignore the `stevmachine-skills` binary
- `go.sum` refreshed via `go mod tidy`
- `scripts/install.sh` pointing to `git@github.com:stevmachine/skills.git`
- `scripts/verify-install.sh` checking `stevmachine-skills` and `~/.stevmachine-skills`

## Verification

- `go build -o stevmachine-skills ./cmd/stevmachine-skills` exits 0
- `packages/claude-plugin/skills/run-stevmachine-skills/smoke.sh` exits 0 and prints `0 failed`
- `./scripts/verify-install.sh` exits 0
- No regressions in the Claude Code install flow (`.claude/skills/stevmachine-jira/` written, `.claude/.mcp.json` written with `${JIRA_API_TOKEN}`)

## Output

Create `.planning/phases/01-rename-and-publish/01-03-SUMMARY.md` when done.
